--synchronized(同步监视器){需要被同步的代码}
--需要被同步的代码即为操作共享数据的代码
--共享数据：即多个线程都需要操作的数据
--需要被同步的代码，在被synchronized包裹以后，就使得一个线程在操作这些代码的过程中，其他线程必须等待
--同步监视器：俗称锁，哪个线程获取了锁，哪个线程就能执行
--同步监视器可以使用任何一个类的对象充当，唯一要求：多个线程必须公用同一个活动监视器
